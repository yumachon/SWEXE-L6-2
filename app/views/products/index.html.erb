<% @products.each do |e| %>
    <p>
        <%= e.name %>
        <%= e.price %>円
        <%= link_to '削除', product_path(e), method: :delete %>
        <%= link_to 'カートに入れる', new_cartitem_path(product_id: e.id) %>
    </p>
<% end %>

    <p>
    <%= link_to '商品を新規登録する', new_product_path %>
    </p>
    <p>
    <%= link_to 'カートの中身を見る', new_cartitem_path %>
    </p>
